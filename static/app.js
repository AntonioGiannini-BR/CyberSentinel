(()=>{'use strict';
const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
const menu=$('#menuToggle'), sidebar=$('#sidebar'); if(menu&&sidebar) menu.addEventListener('click',()=>document.body.classList.toggle('nav-collapsed'));
$('#focusToggle')?.addEventListener('click',()=>document.body.classList.toggle('focus-mode'));
const pass=$('#password'); $('#togglePassword')?.addEventListener('click',e=>{pass.type=pass.type==='password'?'text':'password';e.currentTarget.textContent=pass.type==='password'?'◉':'◎'});
const range=$('#threshold'), rv=$('#thresholdValue'); range?.addEventListener('input',()=>rv.textContent=range.value);
const file=$('#logFile'), dz=$('#dropzone'), fn=$('#fileName'); const showFile=f=>{if(f&&fn)fn.textContent=`${f.name} • ${(f.size/1024).toFixed(1)} KB`}; file?.addEventListener('change',()=>showFile(file.files[0]));
if(dz){['dragenter','dragover'].forEach(x=>dz.addEventListener(x,e=>{e.preventDefault();dz.classList.add('drag')}));['dragleave','drop'].forEach(x=>dz.addEventListener(x,e=>{e.preventDefault();dz.classList.remove('drag')}));dz.addEventListener('drop',e=>{if(e.dataTransfer.files.length){file.files=e.dataTransfer.files;showFile(file.files[0])}})}
$('#uploadForm')?.addEventListener('submit',()=>{const b=$('#analyzeBtn');b.classList.add('loading');b.querySelector('span').textContent='Analisando…'});
$$('.delete-form').forEach(f=>f.addEventListener('submit',e=>{if(!confirm('Excluir esta análise e seus arquivos locais?'))e.preventDefault()}));
$$('.tilt').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(800px) rotateX(${-y*5}deg) rotateY(${x*7}deg) translateY(-2px)`});el.addEventListener('pointerleave',()=>el.style.transform='')});
const data=$('#dashboardData'); if(data){let timeline=[],cats={};try{timeline=JSON.parse(data.dataset.timeline);cats=JSON.parse(data.dataset.categories)}catch(e){}
 const canvas=$('#timelineChart'), empty=$('.empty-chart'); if(canvas&&timeline.length){empty?.remove(); const ctx=canvas.getContext('2d'); const draw=()=>{const dpr=devicePixelRatio||1,w=canvas.clientWidth,h=canvas.clientHeight;canvas.width=w*dpr;canvas.height=h*dpr;ctx.scale(dpr,dpr);ctx.clearRect(0,0,w,h);const pad=34,max=Math.max(1,...timeline.flatMap(x=>[x.analyses,x.alerts]));ctx.strokeStyle='rgba(124,247,255,.09)';ctx.lineWidth=1;for(let i=0;i<5;i++){let y=pad+(h-pad*2)*i/4;ctx.beginPath();ctx.moveTo(pad,y);ctx.lineTo(w-pad,y);ctx.stroke()}const series=(key,color)=>{ctx.beginPath();timeline.forEach((x,i)=>{let px=pad+(w-pad*2)*(timeline.length===1?.5:i/(timeline.length-1)),py=h-pad-(h-pad*2)*x[key]/max;i?ctx.lineTo(px,py):ctx.moveTo(px,py)});ctx.strokeStyle=color;ctx.lineWidth=3;ctx.shadowBlur=14;ctx.shadowColor=color;ctx.stroke();ctx.shadowBlur=0};series('analyses','#67e8f9');series('alerts','#ff4d6d');ctx.fillStyle='#7f91aa';ctx.font='11px system-ui';timeline.forEach((x,i)=>{let px=pad+(w-pad*2)*(timeline.length===1?.5:i/(timeline.length-1));ctx.fillText(x.day.slice(5),px-14,h-8)})};draw();addEventListener('resize',draw)}
 const bars=$('#categoryBars'); if(bars){const max=Math.max(1,...Object.values(cats));Object.entries(cats).forEach(([k,v])=>{const row=document.createElement('div');row.className='signal-row';row.innerHTML=`<div><span>${k}</span><b>${v}</b></div><progress max="${max}" value="${v}">${v}</progress>`;bars.appendChild(row)})}}
setTimeout(()=>$$('.toast').forEach(x=>x.classList.add('hide')),3500);
})();
